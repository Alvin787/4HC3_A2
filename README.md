
  # 4HC3 - A2

  This is a code bundle for 4HC3 - A2. The original project was designed on figma

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  